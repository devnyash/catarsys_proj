feat: mod edit/delete with archive mode, profile page with real data, styled dropdowns, version field

Backend:
- Fix POST 405 (trailing slash)
- Add version column to create/update/serialize
- Add archive/full delete modes to DELETE endpoint
- Add GET /mods/my endpoint
- Migrations: 002 (version column), 003 (archived status)

Frontend:
- PublishModModal: edit mode, styled dropdowns, version field
- New DeleteModModal with archive (badge) and full (hard) delete
- ModCard: show Deleted badge for archived mods
- ModDetailModal: archived badge + block download for archived
- ProfilePage: fetch real mods via API, add edit/delete buttons

Co-Authored-By: Claude <noreply@anthropic.com>