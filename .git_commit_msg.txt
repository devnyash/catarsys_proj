fix: show archived mods in listings, add POST /mods/{id}/restore endpoint

- list_mods and search now include archived mods (IN ('approved', 'archived'))
- New restore endpoint sets status back to 'pending' for re-moderation
- Only the author or an admin can restore an archived mod

Co-Authored-By: Claude <noreply@anthropic.com>
